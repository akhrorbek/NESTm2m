datasource db {
    provider = "postgresql"
    url = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
}


enum Gender {
    male    @map("male")
    female  @map("female")

    @@map("gender")
}